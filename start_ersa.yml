---
- name: Start ERSA Service
  hosts: 192.168.1.221
  become: yes
  tasks:
    - name: Start ERSA service
      ansible.builtin.systemd:
        name: ersa
        state: started
        enabled: yes
